const counter = function(arr)
{
    return ('there are '+arr.length+' cahracter in demon slayer')
}
const printer = function(arr)
{
    return (arr)
}
// module.exports.counter=counter;
// module.exports.printer=printer;

module.exports={
    counter:counter,
    printer:printer
}
